#ifndef ALROGITHMS_H
#define ALGORITHMS_H

#include <iostream>
#include <fstream>

int * bubbleSort(int *a, int len);

int * quickSort(int *a, int lo, int hi);
int partition(int *a, int lo, int hi);



#endif